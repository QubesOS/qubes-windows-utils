# TODO: this will need to compile with both gcc and wdk in odyssey...
# Or make it source-only? A DLL? Build only the qvideo driver with wdk?

TARGETNAME=windows-utils
TARGETTYPE=LIBRARY
TARGETPATH=libs

USE_MSVCRT=1

C_DEFINES = $(C_DEFINES) /D_UNICODE /DUNICODE

INCLUDES=$(INCLUDES); \
    ..\include; \
    $(QUBES_INCLUDES); \
    $(SDK_INC_PATH)\..\ddk;

SOURCES=buffer.c exec.c getopt.c log.c utf8-conv.c

TARGETLIBS=\
    $(SDK_LIB_PATH)\kernel32.lib \
    $(SDK_LIB_PATH)\user32.lib \
    $(SDK_LIB_PATH)\shlwapi.lib \
    $(SDK_LIB_PATH)\wtsapi32.lib
